{
  "name": "piiquante",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "api": "cd packages/api && npm start",
    "web": "cd packages/web && npm run dev",
    "format": "prettier --write ."
  }
}
